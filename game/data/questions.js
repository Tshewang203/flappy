/**
 * Legacy re-exports — quiz data now lives in JSON + quizEngine.js.
 * @deprecated Import from '../utils/quizEngine.js' instead.
 */
export {
  getDifficultyForProgress,
  getRandomQuestion,
  getDifficultyForScore,
  getDeptQuestion,
  getJourneyQuiz,
} from '../utils/quizEngine.js';
